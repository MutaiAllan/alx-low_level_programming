#include "main.h"
#include <stdio.h>

/**
 * mul - gives the product of two numbers
 * @a: inputs one number
 * @b: inputs another number
 * Return: product of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
